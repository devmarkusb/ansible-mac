[defaults]
nocows = True
roles_path = ./roles:/etc/ansible/roles
inventory = inventory
become = true
# community.general.yaml subclasses AnsibleDumper; that breaks on Python 3.14 (TypeError: code must be code, not str).
stdout_callback = default
